# Mountain Path Academy · Time Series Forecasting App
# ---------------------------------------------------
# Install:    pip install -r requirements.txt
# Run:        streamlit run streamlit_forecast_app.py
#
# Python 3.10+ recommended.

streamlit>=1.33,<2.0
pandas>=2.1,<3.0
numpy>=1.26,<3.0
plotly>=5.20,<7.0

# Required by Streamlit's file_uploader / dataframe styling.
# (Pinned loosely so pip can resolve a compatible version with pandas.)
pyarrow>=15.0

# Optional, but greatly improves dataframe styling colour gradients:
matplotlib>=3.7
